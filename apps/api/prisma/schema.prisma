generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model CV {
  id           String   @id @default(cuid())
  originalName String
  internalName String
  path         String
  size         Int
  uploadedAt   DateTime @default(now())
}
