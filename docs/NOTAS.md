Pequeño ajuste para generar diff y permitir la creación de un PR.

Este archivo se añadió únicamente para crear un cambio mínimo en la rama
`feature/cv-upload-view` y poder abrir una Pull Request hacia `develop`.

Dejar tal cual por ahora.